{
  "name": "tune-vote",
  "version": "1.0.0",
  "description": "Music voting application",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "proxy": "node spotify-proxy.js",
    "start-proxy": "npm run proxy"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "react": "^19.1.1",
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  }
}
